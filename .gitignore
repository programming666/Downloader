# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# build files and caches
build/
build-*/
.cache/

# cmake files
CMakeLists.txt.user
CMakeCache.txt
CMakeFiles/
CMakeScripts/
cmake_install.cmake
install_manifest.txt
Makefile
make.bat
CTestTestfile.cmake

# Qt 中间产物（MOC/UIC/RCC 自动生成）
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h

# 编辑器临时/备份
*.bak
*.tmp
*.orig
*.swp
*~

# IDE
.vscode/
.idea/
*.code-workspace

# OS
.DS_Store
Thumbs.db

# installer
# Whitelist IFW metadata + scripts so they're tracked, ignore build outputs.
# Gitignore each output directory individually so whitelist !-rules can re-include
# source files in the parent.
installer/temp/
installer/output/
installer/packages/com.downloader.app/data/
# Un-ignore IFW metadata despite the *.app rule that would otherwise match
# "com.downloader.app" (Qt's reversed-DNS package name).
!installer/packages/com.downloader.app/
!installer/packages/com.downloader.app/meta/
!installer/packages/com.downloader.app/meta/*
# portable/ stays tracked (we ship start.bat + README.txt in the source tree).

# 浏览器扩展产物（源码在 插件/，以下为打包/签名残留）
*.crx
*.pem

# 本地端到端测试数据/产物（test_anti_range_server.py 留作可复用的脚本）
test_anti_range.dat

# 浏览器插件打包产物的 zip（含已打包扩展的本地分发，体积大且可重建）
插件.zip
